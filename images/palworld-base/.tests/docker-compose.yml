version: "3.4"
services:
  linuxgsm:
    image: joshhsoj1902/docker-linuxgsm-images:latest-project-zombiod-base
    environment:
      - LGSM_ADMIN_PASSWORD=password
    network_mode: host
    restart: always
